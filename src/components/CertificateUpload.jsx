// This component is no longer used directly.
// Certificate management is handled by /admin/certificates page (AdminCertificates.jsx).
// Keeping this file to avoid import errors if referenced anywhere.

function CertificateUpload() {
  return null;
}

export default CertificateUpload;
